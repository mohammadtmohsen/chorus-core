{
  "name": "chorus-core",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "The collaboration core shared by Chorus, Chorus Workbench and the Chorus VS Code extension.",
  "license": "MIT",
  "packageManager": "pnpm@11.20.0",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r build && vitest run",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "@types/node": "^22.18.0",
    "typescript": "6.0.3",
    "vitest": "^4.1.10"
  }
}
